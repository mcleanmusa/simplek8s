apiVersion: v1
kind: Pod
metadata:
  name: client-pod
  labels:
    component: web
spec:
  containers: 
    - name: client
      image: mcleanmusa/complex-client
      ports:
        - comtainerPort: 3000
